import React from 'react';
import ReactDom from 'react-dom';

const TitleBlock = ({tb}) => (
        <div className="card grey lighten-3 col s12">
            <h5 className="center-align">{tb}</h5>
        </div>
);
export default TitleBlock;
